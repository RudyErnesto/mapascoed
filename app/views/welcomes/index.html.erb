<!DOCTYPE html>
<html lang="en">

    <meta charset="utf-8">

    <div class="panel panel-warning">
  <div class="panel-heading" style="text-align:center">
    <h3 class="panel-title" ><h5>Lista de Usuarios</h5></h3>
     <form>
      <input name="q" <%= @q %> >
      <button type="submit">Buscar</button>
      </form>
  </div>
  <div class="panel-body">
    <table class="table table-dark">
  <thead>
    <tr>
      <th scope="col">Correo</th>
      
      <th scope="col">Estado</th>
      <th scope="col">Eliminar</th>
    </tr>
  </thead>
  <tbody>
     <% @usuarios.each do |usuario| %>
      <tr>
        <td class = "info"><%= usuario.email%></td>
          <% if usuario.tipo == false %>
            <td class = "success">
              <%= link_to 'Habilitarlo', :controller => :welcomes ,:action => :habilitarlo, :id => usuario.id , :class=>"btn btn-primary"%>
            </td>
          <% else %>
            <td class = "success">
              <%= link_to 'Descativarlo', :controller => :welcomes ,:action => :desactivarlo, :id => usuario.id %>
            </td>
          <% end %>
       <td><%= link_to 'Eliminar',:controller => :welcomes, :action => :eliminar, :id => usuario.id %></td>
       </tr>
    <% end %>
     
  </tbody>
</table>
</div>


    
    </body>

  </div>
   <div class="panel-footer">Usuarios</div>
</div>

</html>

