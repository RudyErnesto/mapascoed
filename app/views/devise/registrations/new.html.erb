
 <div class="content-wrapper" style = "margin-left: 0; width:100%">
    <div class="row ", style='width: 100%;'>
        <div class="col-lg-12">
            <div class="card">
                <div class="card-block">
                    <h4 class="card-title mb-4"></h4>
<%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
  <%= devise_error_messages! %>

  <div class="field">
    <%= f.label :Correo %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" , class:'form-control p-input' %>
  </div>

  <div class="field">
    <%= f.label :Contraseña %>
    <% if @minimum_password_length %>
    <em>(<%= @minimum_password_length %> Caracteres minimos)</em>
    <% end %><br />
    <%= f.password_field :password, autocomplete: "off" , class:'form-control p-input' %>
  </div>

  <div class="field">
    <%= f.label :Confirmar_Contraseña%><br />
    <%= f.password_field :password_confirmation, autocomplete: "off" , class:'form-control p-input' %>
  </div>

  <div class="actions">
    <%= f.submit "Aceptar", :class=>"btn btn-primary" %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
