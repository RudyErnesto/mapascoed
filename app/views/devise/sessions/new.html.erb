 <div class="content-wrapper" style = "margin-left: 0; width:100%">
    <div class="row ", style='width: 100%;'>
        <div class="col-lg-12">
            <div class="card">
                <div class="card-block">
                    <h4 class="card-title mb-4"></h4>

<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="field">
    <%= f.label :Correo %><br />
    <%= f.email_field :email, autofocus: true, autocomplete: "email" , class:'form-control p-input' %>
  </div>

  <div class="field">
    <%= f.label :Contraseña %><br />
    <%= f.password_field :password, autocomplete: "off" , class:'form-control p-input' %>
  </div>

  <% if devise_mapping.rememberable? -%>
    <div class="field">
      <%= f.check_box :remember_me %>
      <%= f.label :Recordarme %>
    </div>
  <% end -%>

  <div class="actions">
    <%= f.submit "Ingresar", :class=>"btn btn-primary"%>
  </div>
<% end %>

<%= render "devise/shared/links" %>
